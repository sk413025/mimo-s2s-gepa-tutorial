# Analyze the latest OpenClaw rollout trajectories with Gemma4.
output_dir: "outputs"

# Leave empty to analyze the latest outputs/*_openclaw_rollout directory.
rollout_run_dir: ""

gemma_base_url: "http://100.70.253.93:8000/v1"
gemma_model: "openai/google/gemma-4-E4B-it"
gemma_api_key: "sk-local"
analyzer_temperature: 0.0
analyzer_max_tokens: 2000
