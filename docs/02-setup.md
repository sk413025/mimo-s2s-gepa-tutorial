# Setup

This tutorial expects two OpenAI-compatible endpoints.

Gemma4 vLLM:

```bash
curl http://100.70.253.93:8000/v1/models
```

MiMo S2S wrapper:

```bash
curl http://127.0.0.1:19080/v1/models
```

Install dependencies if needed:

```bash
pip install -r requirements.txt
```

